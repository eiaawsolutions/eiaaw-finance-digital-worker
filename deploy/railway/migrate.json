{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "pnpm db:migrate && pnpm db:seed",
    "restartPolicyType": "NEVER",
    "numReplicas": 1
  }
}
